<h3>Your task</h3>
<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<% link_to "Delete",
  tasks_path(@tasks),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
